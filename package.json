{
  "name": "elsrainbows-cat",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "webpack --mode production && node scripts/generate-sitemap.mjs",
    "build:dev": "webpack --mode development",
    "watch": "webpack --mode development --watch",
    "watch:dev": "sh -c 'webpack --mode development --watch & python3 -m http.server 5173 --directory dist'",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "css-loader": "^7.1.2",
    "eta": "^4.5.1",
    "html-bundler-webpack-plugin": "^4.23.0",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1"
  }
}
